name: smart_campus_navigator
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # --- THÊM TỪ ĐÂY ---
  # Quản lý trạng thái
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Cơ sở dữ liệu SQLite
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # Tiện ích
  path_provider: ^2.1.1
  shared_preferences: ^2.2.1 # Lưu trạng thái cài đặt lần đầu
  intl: ^0.18.1 # Hỗ trợ đa ngôn ngữ/định dạng ngày tháng
  # -------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true

# --- SỬA PHẦN NÀY ---
  assets:
    - assets/db/          # Để đọc file SQL
    - assets/data/        # Để đọc file JSON
    # - assets/map_tiles/   # Để đọc bản đồ offline sau này
  # --------------------




    # - assets/db/create_schema.sql
    # - assets/data/clusters.json
    # - assets/data/categories.json
    # - assets/data/services_data.json
    # - assets/map_titles


  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
